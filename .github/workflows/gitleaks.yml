name: Gitleaks

on: [push]

jobs:
  gitleaks:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v2
    - name: Run Gitleaks
      uses: zricethezav/gitleaks-action@v4
      with:
        args: "--path=."
